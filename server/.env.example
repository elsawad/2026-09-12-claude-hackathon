# Copy this file to .env (in this server/ directory) and fill in real values.
# .env is gitignored.

# Required for Claude scoring + translation calls (src/scoring.ts, src/translate.ts).
ANTHROPIC_API_KEY=

# Port this Express API listens on. frontend/vite.config.ts proxies /api and
# /uploads here.
PORT=3001
